# 更新记录

## v1.0.0 · 2026-09-02 · X-UI Lite 正式版

- 重构“用户与下载”中的新增用户按钮：使用独立的图标与文字样式，提升对比度、居中对齐和点击识别度。

### 产品与界面

- 重构系统状态的信息层级：运行健康正常时保持简洁，异常时展示具体监测结果。
- 将实时网速与网络连接提升至资源卡片前列，分别提供最近 60 秒上传速率和连接数趋势。
- 新增服务器流量监控：月度额度、每月 1–31 日重置、近 14 天趋势、月底用量预测和余量提示。
- 优化入站列表：流量按面板重置日进入新周期；默认隐藏传输配置；到期时间仅显示日期。
- 将网页管理流程收敛为 VLESS，移除协议列和其他协议的创建入口。
- 恢复入站管理菜单中的二维码与复制功能；支持单独用户管理及 Clash Verge 配置下载。
- 优化所有下拉框的文字垂直居中和筛选区的视觉一致性。
- 新增入站时自动检测本机防火墙策略；UFW 和 firewalld 未放行时可在二次确认后自动添加对应 TCP 规则。

### 设置与可靠性

- 修复流量监控设置保存后仍显示待保存、输入值回空和保存按钮状态不正确的问题。
- “服务器月度流量”统一更名为“流量监控”。
- 新增时区选择、证书关键信息读取和自签名证书生成入口。
- 维持面板端口、证书和访问路径等项目“保存后重启生效”的提示，避免误操作。
- 新增公开仓库一键安装器：下载指定版本、在服务器构建、保留 `/etc/x-ui/x-ui.db` 并备份原运行文件。
- 首次安装使用默认账号 `admin` / `admin`，随机生成访问路径、使用默认端口 `54321`，并在终端输出访问地址与凭据。
- 首次安装访问路径收敛为 5 位大写字母或数字，便于输入和分享，同时保留 `XUI_BASE_PATH` 自定义能力。
- 首次安装增加随机面板访问路径，并在完整 URL 中显示该路径。
- 首次安装自动放行面板 TCP 端口：支持 UFW 与 firewalld 的即时和持久规则；云厂商防火墙仍需在控制台单独配置。
- 一键安装或更新完成后，始终读取当前端口、访问路径和 HTTPS 状态并输出完整控制面板地址；更新时不再显示或重置账号密码。
- 增加 GitHub Actions 多架构发布流程；用户安装时直接下载已编译的 Linux amd64/arm64 包，无需现场安装 Go。

### 兼容性说明

- 此维护版的界面仅支持 VLESS 入站管理。
- 用户管理不会覆盖已有 Reality 等传输参数；请谨慎编辑生产中的连接配置。
- 上游安装脚本不会包含本维护版的界面与功能改动。

## 上游历史记录

2023 年及更早的原始变更记录保留在 [README.md](./README.md) 中，仅供追溯上游历史。
